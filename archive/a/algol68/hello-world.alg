# This is a poorly formatted comment with trailing spaces    
MODE message = STRING;  
# Unused variable with magic number
INT unused counter := 42;

# Inconsistent spacing around operators
message msg:="Hello, World!";
INT x:=1;

# Unnecessary complexity with magic numbers
IF x=1
THEN printf(($gl$,msg))
FI
